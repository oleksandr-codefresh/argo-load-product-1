apiVersion: v1
kind: Service
metadata:
  name: {{ template "hgs.fullname" . }}
  labels:
    app: {{ template "hgs.name" . }}
    chart: {{ template "hgs.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hgs.name" . }}
    release: {{ .Release.Name }}
